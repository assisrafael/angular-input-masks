language: node_js
node_js:
  - '0.10'

branches:
  only:
    - master

before_install:
  - npm install -g bower

before_script:
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

script:
  - gulp test
